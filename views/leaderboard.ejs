<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Meme Leaderboard</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css">
</head>
<body class="bg-gray-100">
    <div class="container mx-auto p-4">
        <header>
            <h1 class="text-2xl font-bold mb-4">Meme Leaderboard</h1>
        </header>
        <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
            <% memes.forEach(meme => { %>
                <div class="bg-white rounded-lg shadow-md p-4">
                    <video class="w-full h-80 object-cover rounded-lg" controls> <!-- Set a max height -->
                        <source src="<%= meme.videoUrl %>" type="video/mp4">
                        Your browser does not support the video tag.
                    </video>
                    <p class="mt-2 text-center">Votes: <%= meme.votes %></p>
                </div>
            <% }); %>
        </div>
        <a href="/" class="mt-4 inline-block bg-blue-500 text-white rounded-lg p-2">Back to Voting</a>
    </div>
</body>
</html>
